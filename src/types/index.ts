export interface IJobTypes {
    id: number;
    title: string;
    isSelected: boolean;
    htmlFor: string;
}

export type Country = {
    id: number;
    name: string;
    code: string;
}

export type Sort = {
    id: number;
    name: string;
}